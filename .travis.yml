# Sample .travis.yml for R projects

language: r
cache: packages
#warnings_are_errors: true
#sudo: required

#r_github_packages:
#  - jimhester/covr

r_github_packages:
  - cran/survival

after_success:
  - Rscript -e 'covr::codecov()'
  